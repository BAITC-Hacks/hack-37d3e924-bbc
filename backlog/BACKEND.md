# Redirected Backlog

This project no longer uses a standalone backend owner for the current hackathon turn.

Use `backlog/APP.md` for Participant 2. APP owns both `backend/**` and `frontend/**`, including upload, storage, worker launch, meeting list/detail, review UI, retry/delete behavior, and DOCX export.

