# Redirected Backlog

This project no longer uses a standalone frontend owner for the current hackathon turn.

Use `backlog/APP.md` for Participant 2. APP owns both `backend/**` and `frontend/**`, including upload, safe result rendering, manual review, source links, status display, and DOCX export.

