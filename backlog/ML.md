# Redirected Backlog

This project no longer uses the old `ML` owner split for the current hackathon turn.

Use `backlog/AI.md` for Participant 1. AI owns `ai/**`, model choice, GPU/resource checks, diarization, transcription, extraction, summary, schema validation, and local offline inference.

