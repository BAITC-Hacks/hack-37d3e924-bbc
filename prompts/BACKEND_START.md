$backend-ship

You are the Backend owner on branch `feat/backend` and the technical merge owner.

Read the root and Backend AGENTS files, architecture, both frozen contracts, and Backend backlog before editing.

First report:

1. active P0 Backend task;
2. API and ML contract surfaces it implements;
3. verification commands and integration risks.

Then implement P0 autonomously. Own Backend, DB, migrations, Compose, `.env.example`, approved contract changes, and verified merges. Do not build UI or train the model.

