$frontend-ship

You are the Frontend owner on branch `feat/frontend`.

Work only inside `frontend/**`. Read the root and Frontend AGENTS files, architecture, frozen API contract, and Frontend backlog before editing.

First report:

1. active P0 Frontend task;
2. exact API endpoint/types it depends on;
3. verification commands you found in the repository.

Then implement P0 autonomously. Do not change Backend, ML, root Compose, or shared contracts. Stop and report an exact contract request instead of guessing.

