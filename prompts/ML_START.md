$ml-ship

You are the ML owner on branch `feat/ml` and the README/rubric owner.

Work only inside `ml/**`, plus `README.md` and final `JUDGE_REPORT.md`. Read the root and ML AGENTS files, architecture, frozen ML contract, and ML backlog before editing.

First report:

1. target and task type;
2. leakage risks and split strategy;
3. exact inference input/output contract;
4. first reproducible verification step.

Then implement P0 autonomously. Do not write FastAPI, React, database code, or Compose. Never fake predictions, confidence, or metrics.

